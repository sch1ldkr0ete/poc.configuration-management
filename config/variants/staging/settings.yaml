apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: configuration-management
  namespace: configuration-management-poc
spec:
  chart:
    spec:
      chart: ./chart
      sourceRef:
        kind: GitRepository
        name: configuration-management-poc
        namespace: flux-system
  interval: 1m0s
  values:
    ingress:
      host: configuration-management-poc.staging.localhost
